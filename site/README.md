# Marketplace site

Next.js app that renders the YAML registry at the repository root.
See [../README.md](../README.md) for the full picture.

```bash
pnpm install
pnpm dev        # http://localhost:3000
pnpm validate   # check the registry YAML against the schema
pnpm test       # loader + schema tests
pnpm build      # static production build
```
