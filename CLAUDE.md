# CHAP Model Marketplace

Curated catalog of chapkit-based epidemiological forecasting models for
[CHAP](https://chap.dhis2.org/) (Climate & Health Analytics Platform, DHIS2).
This git repository is the source of truth and the review gate; the Next.js
site in `site/` is a build-time rendering of it. No auth, no server state.

## Layout

- `registry.yaml` — the model whitelist; every listed model must be runnable
  by a chapkit runtime.
- `models/*.yaml` — one file per model: metadata, configurations, version pins
  (exact 40-char commits). Schema documented in `models/README.md`.
- `benchmarks/` — benchmark record store, one YAML per
  (model, version, dataset). Currently empty: no benchmarks have been run.
- `site/` — Next.js App Router site, pnpm. Loads registry, models, and
  benchmarks at build time through the zod schema in `site/src/lib/schema.ts`;
  invalid data fails the build.

## Commands (run in `site/`)

- `pnpm test` — vitest
- `pnpm lint` — eslint
- `pnpm build` — production build; also validates all YAML through the schema

## Decisions and invariants

- **Chapkit-only.** Only models the chapkit runtimes can execute are listed.
  Guides describe source-based chapkit deployment, not standalone Docker images.
- **Verification semantics.** Merging a pin to `main` IS the 3/3 maintainer
  verification. Open-PR pins render as in-review with n/3 approvals; the site
  never invents proposals.
- **Benchmarks are not run yet, and the methodology is undecided.** How the
  suites will be run (harness, datasets, backtest parameters, ranking) is an
  open question. Never document a benchmark methodology as decided — docs and
  site copy say it will be "added soon". All benchmark views are gated behind
  `BENCHMARKS_LIVE = false` in `site/src/lib/flags.ts`; flip it only when real
  results land in `benchmarks/`.
- **Mock data is labeled.** Catalog sparklines fall back to invented figures
  from `site/src/lib/mock-benchmarks.ts`, always labeled "illustrative" in the
  UI. Anything rendered from real records drops the label automatically.
- `site/AGENTS.md` is regenerated by `next dev` and gitignored — never commit
  it.

## Git conventions

- Commits are SSH-signed and authored by the repo owner alone — no
  `Co-Authored-By` or "Generated with" trailers.
- Never push without being asked.
